import { dappStore } from './dapps';
import { desktopAppStore } from './desktopApp';
import { dynamicConfigStore } from './dynamicConfig';
import { bundleStore } from './bundle';
import { cacheStore } from './cache';

const appStores = {
  dappStore,
  desktopAppStore,
  dynamicConfigStore,
  bundleStore,
  cacheStore,
};

export default appStores;
